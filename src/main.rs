mod retrieve;
mod crawl;
mod util;
mod faucet;

#[tokio::main]
async fn main() {
    // let retriever = retrieve::Retriever::new().await.unwrap();
    // let mut hasher = util::HashUtil::new();
    
}
